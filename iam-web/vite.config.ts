import path from 'node:path'
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    host: true,
    proxy: {
      // No CORS configured on the backend (see iam-api SecurityConfiguration) —
      // proxy same-origin '/api' calls to the Spring Boot server instead.
      // VITE_PROXY_TARGET lets Docker Compose point this at the "backend"
      // service name instead of localhost (see docker-compose.override.yml).
      '/api': {
        target: process.env.VITE_PROXY_TARGET ?? 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/setup.ts',
    css: true,
  },
})
